@using MvcBootstrap.Controllers;

<!DOCTYPE html>
<html ng-app="mvcapp">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/app.css" rel="stylesheet" />
    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="~/Scripts/html5shiv.js"></script>
    <![endif]-->
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="page-header">
        <ul class="nav nav-tabs nav-justified">
            <li class="@(ViewBag.menu == HomeController.MENU ? "active" : null)">@Html.ActionLink("Home", "Index", "Home")</li>
            <li class="@(ViewBag.menu == "About" ? "active" : null)">@Html.ActionLink("About", "About", "Home")</li>
            <li class="@(ViewBag.menu == StudentController.MENU ? "active" : null)">@Html.ActionLink("Students", "Index", "Student")</li>
            <li class="@(ViewBag.menu == CourseController.MENU ? "active" : null)">@Html.ActionLink("Courses", "index", "Course")</li>
            <li class="@(ViewBag.menu == InstructorController.MENU ? "active" : null)">@Html.ActionLink("Instructors", "index", "Instructor")</li>
        </ul>
    </div>
    @Scripts.Render("~/bundles/jquery")
    <script type="text/javascript" src="~/Scripts/bootstrap.min.js"></script>
    <script type="text/javascript" src="~/Scripts/respond.min.js"></script>
    <script type="text/javascript" src="~/Scripts/angular.min.js"></script>
    <script type="text/javascript" src="~/Scripts/ui-bootstrap-tpls-0.6.0.min.js"></script>
    <script type="text/javascript" src="~/Scripts/app.js"></script>
    @RenderBody()
    @RenderSection("scripts", required: false)
</body>
</html>
